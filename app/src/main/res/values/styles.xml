<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="FieldStyle">
        <!--<item name="android:layout_width">70dp</item>-->
        <!--<item name="android:layout_height">70dp</item>-->
        <item name="android:background">@drawable/field</item>
        <item name="android:textSize">@dimen/letter_size</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center</item>
        <!--<item name="android:layout_margin">10dp</item>-->
    </style>

    <style name="StartActivityStyle" parent="Theme.AppCompat.NoActionBar">
        <item name="android:textColor">@color/elements_color</item>
        <item name="android:background">@android:color/white</item>
    </style>

    <dimen name="letter_size">30sp</dimen>
</resources>
